# MINI2+OpenClaw 自巡检 — 2026-06-09 20:39

**触发**: cron job `73bbdaca-9c48-41d7-a333-1c3e2d112c95` (MINI2+OpenClaw自巡检)
**状态**: ⚠️ 发现异常，需要关注

## 整体状态
- ✅ 主机负载正常（1.91 / 2.43 / 2.38）
- ✅ 内存压力正常（66% 空闲）
- ✅ OpenClaw gateway/agent/browser 运行正常
- ✅ 端口 18789/18800 监听正常
- ⚠️ **磁盘空间告急**: Data 卷 `/dev/disk3s5` 使用率 **95%**（403Gi/460Gi）

## 异常详情

### 1. Jira 任务巡查失败（cron job `8d0227c6-23ec-434c-802a-97bdb590dc1f`）
- **错误**: `exec host=sandbox requires a sandbox runtime`
- **原因**: task_state_manager.py 脚本使用了 `host="sandbox"`，但当前 session 未启用 sandbox 模式
- **建议**: 检查 `scripts/task_state_manager.py` 的调用方式，或者启用 `agents.defaults.sandbox.mode`
- **影响**: Jira 任务状态自动巡查无法执行，已连续错误触发 backoff

### 2. 每日股票推荐超时（cron job `5bc40a82-ab3e-4082-ac5a-df1048f3860d`）
- **错误**: 多次 `job execution timed out`（602s / 300s），在 tool-execution-started 阶段
- **原因**: kimi/kimi-for-coding 模型调用超时，可能是网络或模型服务端延迟
- **影响**: 股票推荐功能不可用，连续错误 2 次，backoff 已增至 60s
- **建议**: 检查模型超时配置或增加 fallback 模型超时时间

### 3. 自巡检超时（本 cron job 上一次执行）
- **错误**: 2026-06-09 20:15 左右在 `model-call-started` 阶段超时（300s）
- **状态**: 本次执行成功（20:39）

### 4. 磁盘空间 95%
- **Top 占用**: `Library/Containers` 33G, `.cache` 19G, `Downloads` 9.6G, `Library/Caches` 8.1G
- **风险**: 磁盘满会导致系统不稳定、应用崩溃、日志无法写入
- **建议**: 清理 Docker 容器/镜像、删除旧下载文件、清理缓存

## 历史日志异常（6月8日）
- Jira 域名解析失败：`j.3pigc.com` ENOTFOUND
- `apply_patch` 失败：无效的 patch hunk（`wing_recovery_alert.md`）

## 已采取行动
- 巡检脚本执行成功，报告已写入 `logs/openclaw_self_inspection/latest.md`
- 异常信息已记录到本文件

## 待办
- [ ] 修复 Jira 任务巡查的 sandbox 配置问题
- [ ] 检查股票推荐 cron 的超时配置
- [ ] 清理磁盘空间（Library/Containers 33G 优先）
- [ ] 确认 Jira 域名 `j.3pigc.com` 解析是否正常
