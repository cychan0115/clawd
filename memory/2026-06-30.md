# 2026-06-30 Fix Record

## 一、问题汇总

1. **shequ-cron Pod 异常**，内存占用非常高，疑似导致节点/业务压力增大。
2. **K8S 内部 DNS 解析失败**：
   - 宿主机 ocr1 可以正常 curl 腾讯 COS 文件。
   - 业务 Pod 内 curl 报错：`Could not resolve host`。
   - CoreDNS 日志出现：`read udp <coredns-pod-ip> -> 223.5.5.5/114.114.114.114:53: i/o timeout`
   - 判断为 **CoreDNS 所在节点的 Pod 出口 DNS/网络异常**，不是 COS 地址问题。
3. **部分 Node 的 DHCP/IP 出现异常**，导致节点网络不稳定。
4. **rh2288v5-2288h-v5 / HW 10.0.0.33 主机不稳定**，出现卡死 2 次，怀疑和 NFS 服务/NFS D state 有关。
5. **pg1 / pg1-ai PostgreSQL 一直处于 ContainerCreating**：
   - PVC 使用 `nfs-provisioner-nas`。
   - NFS Server 为 `192.168.31.95`。
   - Pod 被 `nodeSelector` 固定到 `rain`。
   - rain 到 NFS Server 网络不通，报错：`mount.nfs: Network is unreachable`
6. **集群中还有大量 PVC 依赖 nfs-provisioner-nas**，包括 PostgreSQL、Kafka、Zookeeper、MySQL、Elasticsearch、Grafana、JupyterHub、WordPress 等，NFS 是当前较大的存储风险点。

## 二、修复过程

1. **对 shequ-cron 增加内存限制**：
   - 设置 memory limit 为 6Gi。
   - 避免 cron Pod 继续无限制占用内存。
2. **修复 DNS 问题**：
   - 检查 Pod `/etc/resolv.conf`，确认 nameserver 指向 `kube-dns: 10.244.0.10`。
   - 检查 kube-dns endpoints，确认 CoreDNS endpoint 存在。
   - 发现 CoreDNS 在异常节点上时，删除/重建 CoreDNS。
   - 修复节点网络后，重新拉起 CoreDNS。
   - 最终 Pod 内成功解析腾讯 COS 域名，并 curl 返回 `HTTP/1.1 200 OK`。
3. **修复 Node IP/DHCP 问题**：
   - 对丢失或异常的节点 IP 进行 renew / 修复。
   - 恢复 yugi 网络后，CoreDNS 可以正常访问外部 DNS。
4. **对异常节点进行调度隔离**：
   - 当前可调度节点：`en`、`ironman`、`yugi`、`ocr1`
   - 当前暂停调度节点：`newyear`、`rain`、`rh2288v5-2288h-v5`
   - 避免关键服务继续调度到不稳定节点。
5. **停用 NFS 服务**：
   - 因 rh2288v5-2288h-v5 / HW 10.0.0.33 多次卡死，怀疑 NFS 导致机器 D state / 卡死。
   - 暂时关闭 NFS，避免继续拖垮节点。
6. **PostgreSQL/NFS PVC 风险处理**：
   - 已确认 pg1-pvc 和 pg1-ai-pvc 仍在 NFS 上。
   - 暂不直接启动旧 PG。
   - 计划先定位 NFS 原始目录，做冷备。
   - 后续迁移到 rh2288v5-2288h-v5 本地 hostPath/local PV，再新建 PG 实例恢复。

## 三、当前结果

1. 业务 Pod 内 DNS 已恢复。
2. 腾讯 COS 文件在 Pod 内可以正常解析和访问。
3. CoreDNS 已重新运行，当前无明显 DNS 解析失败。
4. yugi 网络已恢复。
5. 异常节点已保持 `SchedulingDisabled`，避免继续影响业务。
6. NFS 已停用，暂时避免继续触发节点卡死。

## 四、遗留风险

1. NFS 仍然是历史存储依赖点，集群中大量 PVC 还绑定在 `nfs-provisioner-nas`。
2. 如果依赖 NFS 的 Pod 重启，可能会再次卡在 `ContainerCreating`。
3. pg1 / pg1-ai 数据还在旧 NFS PVC，需要先冷备再迁移。
4. rain 节点网络到 `192.168.31.95` 不通，不能再承载依赖 NFS 的服务。
5. rh2288v5-2288h-v5 之前出现卡死，需要单独做硬件、磁盘、NFS、内核 D state 排查。
6. fluentd 仍有部分 Pod `CrashLoopBackOff`，后续需要单独处理。

## 五、后续 TODO

1. 导出所有 NFS PVC / PV / Workload YAML，防止误删。
2. 在 `192.168.31.95` 本机冷备 pg1 和 pg1-ai 数据目录。
3. 不删除旧 PVC / PV / NFS 原始目录。
4. 新建 `pg1-local-pvc` 和 `pg1-ai-local-pvc`，使用 rh2288v5-2288h-v5 本地目录。
5. 新建 pg1-local / pg1-ai-local Deployment，先单独启动 pg1-local。
6. PG 启动成功后立即执行 `pg_dumpall`。
7. 再处理 pg1-ai-local。
8. 逐步迁移 Kafka、Zookeeper、MySQL、Elasticsearch、Grafana 等仍依赖 NFS 的服务。
9. 对 `rain`、`rh2288v5-2288h-v5`、`newyear` 逐个修复后再考虑 `uncordon`。
10. 确认 CoreDNS 长期只运行在健康节点上。

## 23:44 Jira 巡查
- Data Center: 处理中=0，待领取=0，AIWH 非完成=0
- Cloud (3pigc.atlassian.net): 仍 404 不可用
- 本地 active task: none
- 结论: 无待处理 Jira 任务
