## Deploy 7_script to SF — 2026-06-01

**Target**: https://win7.cychan.site
**Source**: `/Users/3pigcn/git/7_script`
**Destination**: `sf:/opt/git/7_script`
**Service Port**: 8003 (127.0.0.1)

### What was deployed
- Titan007 Monitor Web UI (FastAPI + SQLite)
- worldcup-monitor (database + scripts)
- titan007.db (88,290 matches, 233 leagues, 825 teams)

### Configuration
- **Nginx**: reverse proxy `win7.cychan.site` → `127.0.0.1:8003`
- **SSL**: Let's Encrypt auto-renewal (expires 2026-08-30)
- **Systemd**: `7win.service`, auto-start on boot
- **Binding**: `127.0.0.1:8003` (internal only, no external exposure)

### Dependency fix
- Initial `pip install` pulled `fastapi 0.136.3` + `starlette 1.2.1`, which broke `Jinja2Templates.TemplateResponse` API
- Downgraded to `fastapi 0.129.2` + `starlette 0.52.1` to match local dev environment
- Added `matplotlib` manually (needed by chart generation scripts)

### Verification
- `curl https://win7.cychan.site/` → HTTP 200 ✅
- systemd status: `active (running)` ✅

### Notes
- Playwright NOT installed on server (per requirement)
- No auto-fetch cron configured (using existing database)
- Port 8000/8001/8002 already occupied on SF; used 8003
